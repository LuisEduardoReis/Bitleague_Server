# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.Application.index
GET     /chat                       controllers.Application.chat

# API
# Users
GET     /api/users                       controllers.Application.getUsers(page: Int ?= 0, page_size : Int ?= 10)
GET     /api/user                        controllers.Application.getUser(id: String ?= "")

POST    /api/user                        controllers.Application.addUser
DELETE  /api/user                        controllers.Application.deleteUser(id: String ?= "")

GET     /api/login                       controllers.AuthenticationController.login
# An example controller showing how to use dependency injection
GET     /count                      controllers.CountController.count
# An example controller showing how to write asynchronous code
GET     /message                    controllers.AsyncController.message

# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET        /assets/*file         controllers.Assets.at(path="/public", file)
#GET        /webjars/*file        controllers.WebJarAssets.at(file)

#->          /                   signalJ.Routes